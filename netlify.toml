[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redireciona SPA (opcional - todas as rotas vão para index.html)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
